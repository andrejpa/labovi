#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

#define MAX 50
struct _Person;
typedef struck _Person*{

}
typedef struct {
	char ime[MAX];
	char prezime[MAX];
	int bodovi;
}student;

int addToFrontOfTheList(Position head) {
	Position newPerson = NULL;
	char name[MAX]
int main()
{
	Person head = { .next = NULL, .next = {0}, .surname}
}